/** Délai avant démarrage auto de la manche suivante (affichage du vainqueur de manche). */
export const ROUND_OVER_AUTO_START_MS = 2000;

/** Durée de la séquence de suppression d'une colonne. */
export const COLUMN_CLEAR_DURATION_MS = 650;

/** Durée d'un vol de carte entre deux ancres. */
export const CARD_FLIGHT_DURATION_MS = 520;

/** Délai entre chaque carte lors de la distribution. */
export const DEAL_STAGGER_MS = 85;

/** Amplitude max de la caméra virtuelle (scale, en ratio). */
export const CAMERA_MAX_ZOOM = 0.018;
